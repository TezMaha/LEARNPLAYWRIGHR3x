let status =0;
console.log(typeof status);

switch(status){


case false :

     console.log("false");
     break;

 case 0:
    console.log("0 matched");  
    
}

//=== strict, value & data typw both of them should be same.
//&& -> and gate
// || -> or gate
//& -> bitwise and operator- never use
// == -> only value should be same, data type can be different
